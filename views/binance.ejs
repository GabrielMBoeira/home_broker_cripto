<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- ChartJS CDN -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

  <link rel="stylesheet" href="css/app.css">

  <title>Trader bot!</title>
</head>

<body>

  <h1>Binance</h1>

  <h1>Binance</h1>

  <div style="height: 500px; width: 600px;">
    <canvas id="myChart"></canvas>
  </div>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"></script>

  <script>
    const labels = [
      'January',
      'February',
      'March',
      'April',
      'May',
      'June',
    ];

    const data = {
      labels: labels,
      datasets: [{
        label: 'My First dataset',
        backgroundColor: 'rgb(255, 99, 132)',
        borderColor: 'rgb(255, 99, 132)',
        data: [0, 10, 5, 2, 20, 30, 45],
      }]
    };

    const config = {
      type: 'line',
      data: data,
      options: {}
    };

    const myChart = new Chart(
      document.getElementById('myChart'),
      config
    );

    const updateData = function () {

      myChart.data.datasets[0].data[0] = myChart.data.datasets[0].data[0] + (20 - Math.floor(Math.random() * (41)))
      myChart.data.datasets[0].data[1] = myChart.data.datasets[0].data[1] + (20 - Math.floor(Math.random() * (41)))
      myChart.data.datasets[0].data[2] = myChart.data.datasets[0].data[2] + (20 - Math.floor(Math.random() * (41)))
      myChart.data.datasets[0].data[3] = myChart.data.datasets[0].data[3] + (20 - Math.floor(Math.random() * (41)))
      myChart.data.datasets[0].data[4] = myChart.data.datasets[0].data[4] + (20 - Math.floor(Math.random() * (41)))
      myChart.data.datasets[0].data[5] = myChart.data.datasets[0].data[5] + (20 - Math.floor(Math.random() * (41)))
      myChart.data.datasets[0].data[6] = myChart.data.datasets[0].data[6] + (20 - Math.floor(Math.random() * (41)))
        
      };

      setInterval(() => {

        updateData();
        console.log(myChart.data.datasets[0].data[0])
        myChart.update();

      }, 2000)

  </script>

</body>

</html>